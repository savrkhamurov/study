using System;

namespace dynamicArray
{
    class Program
    {
        static void Main(string[] args)
        {
            string userInput = "";
            int[] array = new int[0];
            int[] tempArray;
            int numeric;
            int sum = 0;
            bool exit = true;

            while (exit)
            {
                userInput = Console.ReadLine();
                if (userInput == "exit")
                {
                    exit = false;
                }
                else if (userInput == "sum")
                {
                    foreach (int ageSum in array)
                    {
                        sum += ageSum;
                    }
                    Console.WriteLine(sum);
                }
                else
                {
                    numeric = Convert.ToInt32(userInput);
                    tempArray = new int[array.Length + 1];
                    for (int i = 0; i < array.Length; i++)
                    {
                        tempArray[i] = numeric;
                        array = tempArray;
                    }

                }
            }
        }
    }
}

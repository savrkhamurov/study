using System;

namespace sequence
{
    class Program
    {
        static void Main(string[] args)
        {
            int seed = 7;
            int seed2 = 7;
            int limit = 98;
            for (int i = seed; i <= limit; i+=seed2)
            {
                Console.WriteLine(i);
            }
            Console.ReadLine();
        }
    }
}
